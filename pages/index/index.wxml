<!--pages/index/index.wxml-->
<view class="container">
  <!-- 操作按钮行 -->
  <view class="action-row">
    <!-- 搜索按钮 -->
    <view class="search-btn" bindtap="goToSearch">
      <text class="search-icon">🔍</text>
    </view>
    
    <!-- 添加餐厅按钮 -->
    <view class="main-btn" bindtap="goToAddRestaurant">
      <text class="main-btn-text">记录一家新餐厅</text>
    </view>
  </view>

  <!-- 餐厅列表 -->
  <view wx:if="{{restaurants.length > 0}}" class="restaurant-grid">
    <view 
      class="restaurant-card" 
      wx:for="{{restaurants}}" 
      wx:key="_id" 
      bindtap="goToRestaurant" 
      data-id="{{item._id}}" 
      data-name="{{item.name}}"
    >
      <view class="restaurant-name">{{item.name}}</view>
      <view wx:if="{{item.address}}" class="restaurant-address">{{item.address}}</view>
      
      <view class="restaurant-stats">
        <view class="stat-item">
          <view class="stat-label">必点</view>
          <view class="stat-value">{{item.mustTryCount}}</view>
        </view>
        <view class="stat-item">
          <view class="stat-label">避坑</view>
          <view class="stat-value">{{item.avoidCount}}</view>
        </view>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view wx:else class="empty">
    <view class="empty-text">还没有记录餐厅
开始记录你的味觉空间吧</view>
  </view>
</view>
